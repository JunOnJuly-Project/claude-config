# Functional Programming

## 원칙
- **순수 함수**: 동일 입력 → 동일 출력, 부작용 없음
- **불변성**: 데이터 구조는 변경하지 않고 새로 생성
- **고차 함수**: 함수를 인자/반환값으로
- **합성**: 작은 함수를 조합하여 복잡한 로직

## 부작용 격리
IO/DB/네트워크는 경계로 밀어내고, 도메인 로직은 순수하게.

## /develop 통합
`--fp` 플래그 시 reviewer 가 mutability/side-effect 위반을 [중요] 로 표시.
