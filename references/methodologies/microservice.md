# Microservice 패턴

## 기준
- 서비스 = 독립 배포 단위 + 독립 데이터 저장소
- 경계: 바운디드 컨텍스트 (DDD 로부터)
- 통신: 동기(REST/gRPC) 또는 비동기(이벤트 버스)

## 주의
서비스 분리는 **조직이 준비됐을 때**만. 모놀리스 → 모듈러 모놀리스 → 마이크로서비스 순.

## /develop 통합
`--microservice` 플래그 시 planner 가 서비스별 리포/모듈 제안. project-init 이 각 서비스별 CI 파이프라인 생성.
