# {프로젝트명} 프로젝트 로컬 지침

> 이 파일은 전역 `~/.claude/CLAUDE.md` 를 **덮어쓰지 않고 보완**한다.

## 도메인 엄격도 (L1-L3)

| 레벨 | 특성 | 적용 대상 예시 |
|---|---|---|
| **L3** (최엄격) | Clean Arch + ArchUnit + 90% 커버리지 | 결제/주문/보안 핵심 |
| **L2** | DDD 애그리거트 + 80% 커버리지 | 회원/상품 |
| **L1** | 프로토타입 허용 | 프런트엔드/문서 읽기 파사드 |

### 이 프로젝트의 레벨 매핑
- L3: {도메인 목록}
- L2: {도메인 목록}
- L1: {도메인 목록}

## 브랜치 전략
{github-flow | git-flow} — 기본은 github-flow. Git Flow 가 필요하면 명시.

## 커밋 순서 규칙 (L3 전용)
1. 포트 정의
2. 실패 테스트
3. 유스케이스 구현
4. 어댑터 구현
5. ArchUnit + 통합 테스트
6. (선택) 리팩터

각 커밋은 **독립적으로 빌드 통과**해야 한다.

## 금지 사항
- L3 도메인에서 `org.springframework.*`, `jakarta.persistence.*` 직접 import
- 단일 에이전트로 L3 기능 작성 후 커밋 (반드시 `/team-develop` 또는 `/develop` 의 다중 에이전트 파이프라인 사용)
